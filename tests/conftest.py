"""Phase 0 test harness shim.

Intentionally empty. No hash-seed pinning, no environment mutation: the
Phase 0 byte-exact snapshot is deterministic by construction after the
minimum deterministic hardening in governance_agent (see
PATH_C_BOUNDARY.md §8 and the Phase 0 correction review package).
"""
